extend type Query {
#    "Проверка доступности домена"
#    checkDomain(
#        ad_protocol: Int
#        ad_server: String!
#        ad_server_port: Int
#    ): Boolean @field(resolver: "DomainsQueries@checkDomain")
    domains(enabled: Boolean @eq): [Domain!]! @all
    domain(id: ID @eq): Domain @find
}
